name: mpclient
subcommands:
    - add:
        about: add music to library
        args:
            - FILES:
                about: directories and files to add to library
                required: true
                index: 1
                multiple: true
